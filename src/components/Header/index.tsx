import { Container } from './styles'

export const Header = () => {
  return (
    <Container>
      <nav>
        <a href="#about">sobre</a>
        <a href="#projects">projetos</a>
        <a href="#contacts">contato</a>
      </nav>
    </Container>
  )
}